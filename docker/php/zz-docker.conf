[www]
; Force error logging for PHP workers in this pool
php_admin_flag[log_errors] = on

; Enable catch_workers_output so PHP-FPM master process captures worker output and sends it to stderr
catch_workers_output = yes

; Route PHP errors directly to the container's standard error stream (Docker logs)
php_admin_value[error_log] = /proc/self/fd/2
