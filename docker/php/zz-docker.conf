[www]
; Force error logging for PHP workers in this pool
php_admin_flag[log_errors] = on

; Disable catch_workers_output so workers write directly to the log file
catch_workers_output = no

; Route PHP errors to the physical file for separate tailing
php_admin_value[error_log] = /var/www/html/tmp/php_errors.log
